---
audience: [seo]
owner: seguro-tenerife
updated: 2026-09-24
---

# Журнал SEO-решений

Заполняет скилл `seo-analyst` при каждом запуске. Нужен, чтобы через месяц
можно было ответить «помогла ли правка сниппета» и чтобы одна и та же
отвергнутая идея не всплывала каждый день.

Формат записи:

```
## YYYY-MM-DD
Показы 28д: N (дельта) · позиция X.X (дельта) · в индексе: N/M
Задачи: краткий список
Решил не делать: ...
```

<!-- новые записи добавляются ниже, свежие сверху -->

## 2026-09-24 — британский резидентский интент: спрос есть, тем нет (SEGU-38)

Показы 28д по GBR: 729 (2 клика) · позиция 32,5 · разложимо по запросам 571
(21,7 % анонимизировано GSC)

**Что проверялось.** Продуктивная половина находки SEGU-37: под британца, который
на Тенерифе живёт, продукты в каталоге есть (`extranjeros-general`,
`extranjeros-residents`) — есть ли под них спрос, или английская локаль держится
на одном нецелевом туристическом кластере. Разбор:
[`analysis-2026-09-24-en-resident-intent.md`](analysis-2026-09-24-en-resident-intent.md).

**Ответ: спрос измерим, но тем он не даёт.** 83 показа за 28 дней = 14,5 %
разложимого британского трафика против 437 (76,5 %) у туристического ведра.
Выше порога 10 показов — 4 запроса за 28 дней, 9 за 90. И все 9 **уже имеют
страницу**: покрытие резидентского ведра 9 из 9, запросов без страницы ноль.
Узкое место — позиция (47–69 по визовому кластеру) и каннибализация, а не
количество тем.

**Резидентское ведро сжимается, туристическое растёт.** 134 → 83 показа при
позиции по фиксированной корзине 49,0 → 50,7 (+1,6 — **ниже порога 3 пунктов,
сигналом не называю**). Туристическое за тот же период: 192 → 437 показов,
корзина 59,8 → 46,7 (−13,1, сигнал). Просела не выдача, а спрос: исчез
студенческий подкластер (48 показов в предыдущем окне, 0 в текущем) — это
сезонность подачи заявлений под сентябрьский набор, возврата ждать к лету 2027.

**Поправка к методу, которая переживёт эту задачу: страна ≠ интент, и фильтр GBR
структурно не видит британского резидента.** Он живёт на Тенерифе и ищет из
Испании — попадает в срез ESP. Пришлось добавить второй срез (все страницы
`/en/` по странам): GBR 501 · ESP 101 · USA 72 · IRL 45 за 28 дней. Из него две
вещи, которых срез по GBR дать не мог: англоязычный резидент в Испании даёт
всего два запроса выше порога, зато **США — третья страна локали, и её трафик
чище британского** (визовый кластер DNV и студенческий, турист миноритарно). По
прецеденту SEGU-23 американец, оформляющий визу в Испанию, — наш лид.

**Ирландия:** 78 показов за 90 дней, из них туристических 98,2 %, резидентских
**ноль**. Отдельной строкой набралась и ответ дала однозначный.

Задачи: новых тем в очередь — **ноль** (см. ниже). В месячный слой 2026-10-01
передано три наблюдения: пара `private-vs-public-healthcare-spain` /
`private-healthcare-tenerife` делит 78 показов по двум запросам (кандидат на
`объединить`); `/en/blog/digital-nomad-visa-insurance/` стоит на 55,2 при
испанском двойнике на 32,2 (кандидат на `углубить`, кластер проходит условие 2
целиком); `complete reimbursement private insurance spain` — 17 показов,
позиция 11,0, единственный свободный резидентский запрос на расстоянии удара
во всей локали. Вердиктов не выношу — не мои права.

Инструмент: `scripts/analytics/intent.py` — раскладывает страновой срез по
вёдрам интента и считает сдвиг по фиксированной корзине. Заведён потому, что
руками такое разложение делается один раз и не воспроизводится, а решение
SEGU-37 имеет условия пересмотра, которые нужно будет перепроверять.

**Решил не делать.** (1) Не внёс ни одной темы в очередь, хотя DNV-кластер
проходит все три проверки условия 2: страница под него существует, и вторая
статья добавила бы четвёртую страницу к уже зафиксированной каннибализации.
Пустой результат здесь честнее выдуманной темы — от этого правило отбора и
защищает. (2) Не стал чинить каннибализацию и не переписывал сниппеты:
`объединить` и `углубить` — вердикты жизненного цикла, их выносит `/seo-monthly`.
(3) Не тронул `/en/blog/travel-insurance-tenerife/` — заморожена решением
SEGU-37. (4) Не выносил вопросов владельцу: ни одного, требующего юрлица, денег
или доступов, задача не подняла.

## 2026-09-24 — день без поломок; крупнейший кластер сайта упёрся в вопрос о продукте

Показы 28д: 2824 (+1283) · клики 10 (+6) · позиция на фиксированной корзине 50.6 → 43.1
(−7.5, выдача выросла) · в индексе 175/188 · негативный контроль 404 ✅

**Доступы.** `healthcheck.py`: 6 проверок, отказов 0, предупреждений 0. Токен GSC живой,
последний день с данными 2026-09-21 (отставание 3 дн.), PostHog отвечает. Инвариант
«sitemap vs репозиторий» сошёлся: 188 URL в sitemap, все 164 статьи репозитория на месте —
невыкаченного контента нет.

**Трафик.** 28 дней (2026-08-26 — 2026-09-22): показы 2824 (+1283, +83 %), клики 10 (+6),
CTR 0.35 %. 7 дней (2026-09-16 — 2026-09-22): показы 791 (−144, −15 %), клики 3 (+2) —
свежего обрыва нет, эскалация «показы упали вдвое» не сработала.

**Движение выдачи.** `positions.py`, корзина 44 запроса в обоих периодах, покрытие 43 %
текущих показов: 50.6 → 43.1 (−7.5 пункта), вклад смены микса −3.8. Сдвиг выше порога
значимости, и он не объясняется одним приходом новых запросов. «Официальная» средняя GSC
(55.4 → 44.2) как вывод не подаётся.

**Индексация.** 175 из 188 в индексе. Вне индекса 13, новых выпадений нет:

| Что | Сколько | Статус |
|---|---|---|
| `cookies` / `privacy` / `terms` (4 локали) | 12 | «URL неизвестен Google» — юридические страницы, на трафик не влияют, вопрос о выводе их из sitemap открыт к владельцу с 2026-09-05 |
| `/en/` | 1 | «копия, каноникалы не совпадают» — разметка проверена и корректна, это решение Google на молодом домене; перепроверка 2026-10-05, как записано 2026-09-05 |

Новых публикаций за сутки нет: последняя — `family-reunification-visa-insurance` (4 локали,
2026-09-23), она выкачена и уже собирает показы (`/es/` — 36 пок., поз. 15.1, 1 клик).

**Воронка (PostHog, 28 дн.).** 105 событий, 12 сессий; каналы: search 8, direct 5. До чата
дошли 4 сессии, `question_asked` 2, `answer_received` 2, `chat_handoff_offered` 2,
`handoff_clicked` 0, `lead_submitted` 0. Окно захватывает 2026-09-14, до которой
`chat_started` терялся (SEGU-18), поэтому сравнение с предыдущим периодом не делается.

**Внешний съём (Rush Analytics).** Сегодня не требовался: новых тем в постановку не шло,
спорных форматов страниц не разбиралось. Баланс не тратился.

### Находка дня: 18 % показов сайта — кластер, под который может не быть продукта

`/en/blog/travel-insurance-tenerife/` — 513 показов за 28 дней (18 % всех показов сайта),
**0 кликов**. Средняя по странице 19.8 обманчива: она смешана с мелкими странами, где
1–4 показа. Разложение по странам (покрытие 411/513 = 80 %):

| Страна | Показы | Позиция |
|---|---|---|
| Великобритания | 323 | 24.3 |
| Ирландия | 44 | 22.1 |
| США | 10 | 17.6 |
| остальные | по 1–4 | шум |

Страница растёт, и быстро: `is tenerife in europe for travel insurance` за месяц прошла
7 пок./поз. 56.4 → 89 пок./поз. 11.3 (из них GBR 74). Весь видимый кластер — 411 показов
по 12 запросам, и все они одного интента: британский или ирландский турист перед вылетом
выясняет, считается ли Тенерифе Испанией и Европой для его тревел-полиса.

Проблема не в сниппете и не в глубине текста — разделы под эти вопросы в статье уже есть,
H2 совпадают с запросами дословно. Проблема в том, что **линия `viaje` (5 продуктов) —
это полисы испанского страховщика для выезжающих из Испании и для краткосрочного шенгена.**
Британец, летящий на неделю в Лос-Кристианос, покупает полис дома. То есть самый крупный
кластер сайта, возможно, не конвертируется в принципе, и нулевой CTR при позиции 11 —
скорее симптом нерелевантности, чем слабого сниппета.

Разбор от 2026-09-09 (`candidates-2026-09-09.md`, «Британский трафик … вопросов не
вызывает: это резиденты и туристы на Тенерифе, ровно наша аудитория») это утверждал, но
продуктом не проверял. Проверять его — не дневной слой: это вердикт о ценности страницы.
Вынесено подзадачей на Bencomo с данными под решение.

### Задач копирайтеру сегодня нет

Единственный крупный кандидат — та же страница, и до ответа по продукту любая правка там
будет вложением вслепую. Второй кандидат проверен и отклонён:
`/es/blog/what-counts-as-accident-insurance-spain/` — 39 показов, поз. 7.4, 0 кликов,
формально «слабый сниппет» по таблице решающих правил. Но из 39 показов Google открывает
только 17, и главный видимый запрос — 8 показов на
`¿qué característica define técnicamente un accidente… evento súbito, externo y violento`.
Это вопрос из экзамена на страхового агента, а не покупатель. **Коммерческого намерения
нет, сниппет не переписываем.** Запись нужна, чтобы эта страница не всплывала кандидатом
каждую неделю.

Остальные сигналы «на расстоянии удара» уже в работе: SEGU-21 (`health-insurance-cost-spain`),
SEGU-22 (`reimbursement-health-insurance-spain`), SEGU-29 (`dental-implants-insurance-spain`) —
все три в `in_review`, дублировать нечего.

**Решил не делать:** не трогал `travel-insurance-coming-to-spain` ↔ `travel-insurance-tenerife`
(предварительный вердикт по паре — не раньше 2026-11-09, и это месячный слой); не выносил
вердикт по эксперименту от 2026-09-05, хотя замер в его пользу (`tenerife travel insurance`
64.7/126 → 26.8/57 на странице-приёмнике) — вердикты выносит `/seo-monthly`.

Доказательство: этот коммит; замеры `report.py --days 28/7 --compare`, `positions.py --days 28`
(общий, `--page /en/blog/travel-insurance-tenerife/`, `--query "is tenerife in europe for
travel insurance" --segment country`), `indexation.py --limit 200`, негативный контроль 404;
подзадача на Bencomo по кластеру `viaje`.

## 2026-09-23 — мягкая 404 на проде: фикс выкачен, проверен на живом сайте

Не недельный разбор, а запись о дне фактического деплоя (критерий приёмки
SEGU-31). Цифр по трафику здесь нет намеренно: правка выкачена сегодня, судить
по ней рано.

**Что было.** Любой несуществующий адрес отвечал `HTTP 200` и рендерил русскую
главную — 49 669 байт, без `noindex`. Четыре дня подряд: 21, 22, 23 сентября.

**Почему стояло четыре дня, а не один.** Фикс был написан 2026-09-21, но лежал
в ветке `docs/seo-transfer-pack` (SEGU-25, `in_review`) и в `main` — откуда
собирается прод — не попадал ни дня. Дневные прогоны 21 и 22 сентября записали
симптом как «фикс не выкачен» и стали ждать деплоя, которому нечего было
забирать. Сверку ветки с `origin/main` никто не сделал, потому что рутина её не
требует: `check_sitemap()` в `healthcheck.py` сравнивает живой sitemap с
**рабочим деревом**, а не с `origin/main`, и оба состояния — «лежит в чужой
ветке» и «влито, ждём деплой» — дают одну и ту же строку отчёта. Вторая дыра
глубже: `check_sitemap` смотрит только на статьи, а `404.astro` не статья —
его отсутствие наша проверка не поймала бы в принципе. Обе закрываются в
SEGU-32.

**Что выкачено.** Один новый файл `frontend/apps/web-astro/src/pages/404.astro`
→ `dist/404.html`. Без него Cloudflare Pages включает SPA-фоллбэк и отдаёт
`index.html` на любой путь. Из коммита `c00fcbc` взят только этот файл; мост
ссылки `](#chat)` и планка перелинковки в валидаторе уедут вместе с SEGU-25.

**Деплой:** PR #2 → `main` (`00e88ce`), `deploy-frontend.yml` прогон
`35870889755`, `conclusion=success`, 2026-09-23.

**Проверено на живом сайте после выката:**

| Проверка | Было | Стало |
|---|---|---|
| несуществующий URL, код | `200` | **`404`** (три разных адреса подряд) |
| несуществующий URL, размер тела | 49 669 байт (дубль главной) | 1 758 байт |
| `<meta name="robots">` на нём | отсутствовал | `noindex` |
| `/`, `/es/`, `/en/`, `/uk/`, `/es/blog/`, `/robots.txt` | 200 | 200 — не задето |
| URL в `sitemap-0.xml` | 188 | 188 — страница 404 в карту не попала |

**Чего ждать от этого в цифрах.** Прямого прироста показов не ждём: правка не
добавляет страниц и не меняет ни одного существующего URL. Она снимает риск —
раньше любая опечатка в чужой ссылке становилась индексируемым дублём главной,
а `/` сейчас единственная страница с кликами (1 клик, 105 показов за 28 дней).
Побочно чинится проверка «статья опубликована»: до сегодня 200 отдавал и
невыкаченный адрес, так что зелёный ответ ничего не доказывал.

**Решил не делать:** не вливать ветку `docs/seo-transfer-pack` целиком —
8 из 9 её коммитов (SEGU-25/23/22/16) в `in_review` и мной не приняты, выкатывать
их ради 404 нельзя. Не переносить валидатор из `c00fcbc`: его планка действует
для статей от 2026-09-22, а четыре файла `family-reunification-visa-insurance.md`
на `main` стоят с `updated: 2026-09-23` без ссылки `](#chat)` — проверено,
дал бы 4 ошибки сразу.


## 2026-09-08 — неделя (2026-08-31 — 2026-09-06)

Показы: **559** (+198, +55%) · клики **4** (+3) · CTR **0.72%** · позиция **42.4**
(ухудшилась на 5.8) · в индексе **171/184** (+1). Предыдущая неделя: 2026-08-24 —
2026-08-30, 361 показ, 1 клик, CTR 0.28%, позиция 36.7.
PostHog 7д: 9 сессий, из них 2 из поиска; 1 `chat_started` → 0 лидов.

### Главное за неделю: работа от 2026-09-05 не задеплоена

`healthcheck.py` дал отказ по инварианту «sitemap vs репозиторий»: 4 статьи есть в
репозитории и нет в живом sitemap. Проверка на живом сайте подтвердила шире — **не
задеплоено вообще ничего из пяти коммитов 2026-09-05**:

| Что правили | В репозитории | На сайте |
|---|---|---|
| `/es/blog/funeral-insurance-spain/` title | «Seguro de decesos **en Tenerife**…» | «Seguro de decesos **en España**…» |
| `/es/blog/pet-insurance-spain/` title | «Seguro de mascotas **en Tenerife**…» | «Seguro para mascotas **en España**…» |
| `/es/blog/digital-nomad-visa-insurance/` title | «**Seguro DNV**: póliza médica…» | «Seguro para el visado de nómada digital (DNV)…» |
| `/en/blog/travel-insurance-tenerife/` (новая статья) | есть, 4 локали | soft-404 (см. ниже) |

Косвенное подтверждение из GSC: у всех четырёх «чистых» замеров H1 позиция за
неделю не сдвинулась ни на пункт — `seguro decesos tenerife` 11.9 → 11.9,
`seguros de mascotas en tenerife` 12.0 → 12.0, `accident cover in spain`
13.7 → 13.7, `seguro dnv` 4.7 → 4.7. Google видит старые страницы.

**Следствие для экспериментов:** отсчёт 14 дней до вердикта по H1 и H2 не начался.
Дата вердикта 2026-10-05 недействительна — считать надо от дня фактического
деплоя. В `experiments.md` это зафиксировано.

### Побочная находка: сайт отдаёт soft-404

Любой несуществующий URL — `/zzz-junk-path/`, `/en/blog/zzz-nonexistent/` — отвечает
**HTTP 200** и рендерит русскую главную (`<title>Страховка для ВНЖ и визы в Испании —
Тенерифе</title>`). Причина: в сборке нет `dist/404.html` (нет `src/pages/404.astro`),
а воркер Cloudflare Pages отдаёт всё через `env.ASSETS.fetch`, который в этом случае
падает на индекс. Для Google это «мягкая 404» — он может индексировать мусорные URL
как дубли главной. Правится одной страницей `src/pages/404.astro`; отдельным деплоем
смысла нет, логично добавить в тот же выкат.

### Тренд

Показы по неделям за 8 недель: 69 → 90 → 111 → 384 → 301 → 625 → 361 → 559.
Рост есть, но пилой: разброс ±40% неделя к неделе на нашем объёме — это не тренд,
а шум индексации. Средняя позиция всё это время держится в коридоре 36.7–45.4 и
никуда не идёт. Рост показов +55% при ухудшении позиции на 5.8 объясняется просто:
новые показы пришли в глубокий хвост (`/es/blog/health-insurance-cost-spain/` — 67
показов на позиции 56, `dental-implants-insurance-spain` — 28 на 65.9), а средняя
позиция взвешена по показам. Это не регресс ранжирования.

### Новое в спросе (для дневной рутины, не задача этой недели)

Впервые за неделю проявился ценовой кластер на испанском, целиком свой рынок:
`cuanto cuesta un seguro medico` (10 пок., **поз. 17.8**), `precio seguro medico
familiar` (8, 86.2), `cuanto cuesta un seguro de salud` (7, 93.7), `cuanto vale un
seguro médico al mes` (7, 79.9). Садится на `/es/blog/health-insurance-cost-spain/`
(67 пок., поз. 56.0). Продукт есть — линия `salud`. Цен не публикуем: работа тут —
глубина ответа про факторы стоимости, а не цифры.

### Задачи копирайтеру: ноль, сознательно

Разбор запросов на расстоянии удара сделан, кандидаты есть (`cuanto cuesta un seguro
medico` 17.8, `seguro digital nomad visa` 11.2, `/en/blog/reimbursement-health-insurance-spain/`
11.3). Задач всё равно не ставлю: **в очереди на выкат уже лежит неделя написанного
текста, которого Google не видит.** Пока деплой не прошёл, каждая новая статья только
увеличивает недоставленный запас и делает будущие замеры «до/после» ещё грязнее.
Возвращаемся к задачам первым запуском после деплоя.

### Решил не делать

- **Не ставить три задачи «потому что положено по регламенту».** Причина выше:
  узкое место недели — доставка, а не производство текста.
- **Не выносить вердикт по H1.** Срок (2026-10-05) не наступил, и, что важнее,
  правка не доехала до Google — сравнивать нечего.
- **Не чинить soft-404 и не заводить `404.astro` самому.** Это фронтенд + выкат,
  решение владельца; без деплоя правка всё равно не сработает.
- **Не трактовать ухудшение позиции на 5.8 как регресс.** Разложение по страницам
  показывает приток хвостовых показов, а не падение существующих позиций.
- **Не трогать 12 юридических страниц вне индекса** (`/privacy/`, `/terms/`,
  `/cookies/` × 4 локали). На трафик не влияют, вопрос об их присутствии в sitemap
  открыт к владельцу с 2026-09-05 и ответа не требует срочно.

### Вывод на следующую неделю

Ничего не пишем и не планируем до выката: задача недели — довести пять коммитов от
2026-09-05 до продакшена, после чего заново снять срез и запустить отсчёт по H1/H2.

### Дополнение того же дня: деплой сделан, правило изменено

Владелец ответил, что выкатывать нужно самостоятельно и разрешения не спрашивать.
Причина найдена и оказалась проще, чем «деплой не сработал»: **автодеплой исправен
и всегда был исправен**, `.github/workflows/deploy-frontend.yml` срабатывает на push
в `main` — но вся SEO-работа коммитилась в ветки (`feat/seo-daily-pipeline`,
`fix/posthog-exclude-admin`), а `main` три дня стоял на `d6f7f24`. Ветка выглядела
как выполненная работа: коммиты есть, в очереди тем стоит «опубликовано».

Сделано 2026-09-08: `pnpm build:web` + `build:admin` зелёные, тесты 22/22, ветка
влита в `main` fast-forward и запушена (`d6f7f24..f8554d6`). Автодеплой отработал
за ~45 секунд. Проверка на живом сайте: sitemap **184 → 188 URL**, все шесть
правленых title отдаются новыми, `travel-insurance-tenerife` открывается на всех
четырёх локалях. `healthcheck.py`: отказов 0.

**Процессная правка:** из `pipeline.md` убрана строка «статьи не в живом sitemap →
вопрос к релизу, не к SEO» — именно она разрешала не довозить работу. Вместо неё
раздел «Деплой — шаг рутины» с командами и правилом «приёмка по живому сайту, а не
по зелёному CI». Отсчёт по H1/H2 идёт с 2026-09-08, вердикт не раньше 2026-09-22.

Что это меняет в выводе выше: запрет ставить задачи копирайтеру снят — он был
следствием переполненной очереди на выкат, которой больше нет.

## 2026-09-05 — стартовый аудит органики (SEGU-4)

Показы 28д (2026-08-07 — 2026-09-03): **1630** (+1070) · клики **4** · CTR **0.25%**
· позиция **41.0** (−0.8) · в индексе **85 из 90 проверенных** (в sitemap 184 URL).
PostHog 28д: 33 сессии, из них органика 2; воронка чата 1 `chat_started` → 0 лидов.

### Главный вывод: узкое место — позиция, а не сниппет

Распределение показов по позициям (срез «запрос × страница», 1119 показов из 1630;
остальное GSC анонимизирует):

| Позиция | Показы | Доля |
|---|---|---|
| 1–3 | 1 | 0.1% |
| 4–10 | 60 | 5.4% |
| 11–20 | 40 | 3.6% |
| 21–50 | 301 | 26.9% |
| 51+ | 717 | 64.1% |

91% показов приходится на позиции ниже 20-й, 64% — ниже 51-й. При средней позиции 41
ожидаемый CTR — те же 0.1–0.3%, что мы и видим. **Сниппеты не мешают кликам: мешает
то, что почти весь показ происходит там, где не кликают вообще.** Даже идеальный CTR
на всём, что стоит на позициях 4–20, даёт порядок 5–8 кликов в месяц — это потолок
правки сниппетов, а не стратегия.

### Гипотеза о гео-модификаторе (проверяем)

Спрос сформулирован с гео («… en Tenerife», «… in Tenerife»), а статьи названы
«… en España». Страницы, у которых гео есть в slug/title, держатся на позициях 5–20
(`private-healthcare-tenerife` 10.8, `family-insurance-tenerife` 19.4,
`moving-to-tenerife-checklist` 5.9); страницы с «-spain» под теми же гео-запросами
стоят на 30–80 (`life-insurance-spain` 73–81 под «seguro de vida en tenerife»).
Это корреляция на малой выборке, не закон. Проверяем экспериментом — записи в
`experiments.md` от 2026-09-05.

### План на месяц, три ведра

**Ведро 1 — переписать title+description** (эффект малый и быстрый, трудоёмкость часы):

| Стр. | Целевой запрос | Поз. / показы |
|---|---|---|
| `/es/blog/digital-nomad-visa-insurance/` | `seguro dnv` | 4.7 / 28 |
| `/es/blog/funeral-insurance-spain/` | `seguro decesos tenerife` | 11.9 / 15 |
| `/es/blog/pet-insurance-spain/` | `seguros de mascotas en tenerife` | 10.3 / 28 (стр.) |
| `/en/blog/accident-insurance-spain/` | `accident cover in spain` | 13.7 / 3 |
| `/en/blog/reimbursement-health-insurance-spain/` | `reimbursement rehabilitation spain` | 20.2 / 8 |

**Ведро 2 — дополнить существующую статью** (эффект средний, трудоёмкость день на статью):

| Стр. | Кластер | Показы кластера |
|---|---|---|
| `/en/blog/private-vs-public-healthcare-spain/` | public healthcare Tenerife | 98 |
| `/es/blog/life-insurance-spain/` | seguro de vida en Tenerife | 90 |
| `/es|/en/blog/student-visa-insurance-spain/` | student visa insurance | 155 |
| `/es|/en/blog/digital-nomad-visa-insurance/` | DNV 2026 / requirements | 232 |
| `/es/blog/family-reunification-visa-insurance/` | reagrupación familiar | 28 |

**Ведро 3 — новая тема** (эффект большой, трудоёмкость 4 локали):

| Тема | Кластер | Показы |
|---|---|---|
| `travel-insurance-tenerife` (EN-ведущая) | tenerife travel insurance | 236 |

### Решил не делать

- **Отдельную страницу про public healthcare в Тенерифе** — каннибализировала бы
  `private-vs-public-healthcare-spain`, которая уже собирает весь кластер и стоит
  на 5.6 по «what is a public health charge in tenerife». Сначала углубляем её.
- **Отдельную «Seguro de vida en Tenerife»** — по той же причине, гео-раздел уходит
  в `life-insurance-spain`.
- **Темы из `suggest.py`** — из 25 непокрытых испанских подсказок все 25 либо про
  Латинскую Америку, либо брендовые (Adeslas). Полезных подсказок ноль; спрос в этом
  проекте читается из Search Console, а не из `suggest.py`. Это стоит помнить.
- **Правку canonical/hreflang** — разметка проверена на живом сайте и корректна
  (self-canonical + 4 hreflang + x-default на всех трёх проверенных URL). Вердикт GSC
  «`/en/` — копия, канонические версии не совпадают» — решение самого Google на
  молодом домене, а не наш баг. Перепроверить через 30 дней.
- **Гео-лендинги по городам** (Adeje, Arona, Los Cristianos, Santa Cruz, La Laguna) —
  ни одного показа за 28 дней, спрос не подтверждён, правило отбора не выполнено.

### Не наша зона / открытые вопросы владельцу

- `/en/privacy/`, `/en/terms/`, `/cookies/`, `/en/cookies/` — «URL неизвестен Google».
  Юридические страницы, на трафик не влияют; если хочется убрать из отчёта — вопрос
  к владельцу, надо ли вообще держать их в sitemap.
- В `keywords` нескольких статей засеяны опечатки (`accidnet insurance`,
  `acident insurance`, `seguro de bida`). Google `meta keywords` игнорирует — вреда
  нет, но мусор стоит вычистить попутно.

