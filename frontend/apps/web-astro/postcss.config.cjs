/**
 * PostCSS-конфиг Astro-приложения: Tailwind + autoprefixer.
 * .cjs — чтобы работать в ESM-пакете ("type":"module").
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
